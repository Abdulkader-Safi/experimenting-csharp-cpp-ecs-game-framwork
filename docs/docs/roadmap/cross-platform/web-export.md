# Web Export

:::info Planned
This feature is not yet implemented.
:::

Run the engine in a web browser via Emscripten + WebGPU/WebGL.

## Planned Scope

- Emscripten compilation of C++ renderer
- WebGPU or WebGL backend (replacing Vulkan)
- C# via WASM (Blazor or similar)
- HTML5 canvas for rendering
