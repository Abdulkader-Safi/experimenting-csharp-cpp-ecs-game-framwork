# Input Action Mapping

:::info Planned
This feature is not yet implemented.
:::

Map physical keys to logical actions ("Jump" = Space / A-button).

## Planned Scope

- Action definition (name + list of bindings)
- Support keyboard, mouse, and gamepad bindings
- Query actions by name instead of raw key codes
- Rebindable at runtime
- Configuration file for input mappings
